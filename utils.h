#ifndef UTILS_H
#define UTILS_H

#include <cmath>
#include "mit.h"

// Forward declaration
struct point;

double calculate_distance(point a, point b);
double calculate_vector_length(point a);

#endif
